# 🏗️ System Architecture

## High-Level Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     SMART AMBULANCE SYSTEM                      │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐                    ┌──────────────────────┐
│   FRONTEND (React)   │                    │  BACKEND (Spring)    │
│   Port: 5173         │                    │  Port: 8080          │
│                      │                    │                      │
│  ┌────────────────┐  │                    │  ┌────────────────┐  │
│  │  MapComponent  │  │                    │  │  Controller    │  │
│  │  (Leaflet)     │  │                    │  │  /api/*        │  │
│  └────────────────┘  │                    │  └────────────────┘  │
│                      │                    │          │           │
│  ┌────────────────┐  │   HTTP REST API    │  ┌────────────────┐  │
│  │  Dashboard     │◄─┼───────────────────►│  │  Service       │  │
│  │  (Status)      │  │                    │  │  (Business)    │  │
│  └────────────────┘  │                    │  └────────────────┘  │
│                      │                    │          │           │
│  ┌────────────────┐  │   WebSocket        │  ┌────────────────┐  │
│  │  WebSocket     │◄─┼───────────────────►│  │  WebSocket     │  │
│  │  Client        │  │   (SockJS+STOMP)   │  │  Config        │  │
│  └────────────────┘  │                    │  └────────────────┘  │
│                      │                    │          │           │
│  ┌────────────────┐  │                    │  ┌────────────────┐  │
│  │  Geolocation   │  │                    │  │  Repository    │  │
│  │  API           │  │                    │  │  (JPA)         │  │
│  └────────────────┘  │                    │  └────────────────┘  │
└──────────────────────┘                    │          │           │
                                            │  ┌────────────────┐  │
                                            │  │  H2 Database   │  │
                                            │  │  (In-Memory)   │  │
                                            │  └────────────────┘  │
                                            └──────────────────────┘
```

---

## Data Flow

### 1. Initial Load
```
Frontend                Backend                 Database
   │                       │                        │
   │──── GET /signals ────►│                        │
   │                       │──── findAll() ────────►│
   │                       │◄──── 3 signals ────────│
   │◄──── JSON response ───│                        │
   │                       │                        │
```

### 2. Emergency Start
```
Browser                Frontend              Backend
   │                      │                    │
   │─ Allow Location ────►│                    │
   │                      │                    │
   │◄─ Get Position ──────│                    │
   │                      │                    │
   │── Coordinates ───────►│                    │
   │                      │                    │
   │                      │─ POST /location ──►│
   │                      │                    │
   │                      │◄─ Response ────────│
   │                      │                    │
```

### 3. Real-time Updates
```
Backend                WebSocket              Frontend
   │                       │                      │
   │─ Signal Update ──────►│                      │
   │   (GREEN)             │                      │
   │                       │─ Broadcast ─────────►│
   │                       │   /topic/signals     │
   │                       │                      │─ Update UI
   │                       │                      │─ Show Alert
```

---

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│                        App.tsx                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  State Management                                    │   │
│  │  - ambulancePos                                      │   │
│  │  - signals                                           │   │
│  │  - lastResponse                                      │   │
│  │  - isEmergencyActive                                 │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                             │
│  ┌─────────────────────┐      ┌─────────────────────┐      │
│  │   MapComponent      │      │    Dashboard        │      │
│  │                     │      │                     │      │
│  │  - Leaflet Map      │      │  - Signal List      │      │
│  │  - Ambulance Marker │      │  - Distance Card    │      │
│  │  - Signal Markers   │      │  - Time Saved Card  │      │
│  │  - Auto Pan         │      │  - Alert Popup      │      │
│  └─────────────────────┘      └─────────────────────┘      │
│                                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  WebSocket Connection                                │   │
│  │  - SockJS + STOMP                                    │   │
│  │  - Subscribe: /topic/signals                         │   │
│  │  - Auto-reconnect                                    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## Backend Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                   AmbulanceApplication                      │
│                   (Spring Boot Main)                        │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
┌───────▼────────┐  ┌───────▼────────┐  ┌──────▼──────┐
│  Controller    │  │  WebSocket     │  │  Repository │
│  @RestController│  │  Config        │  │  (JPA)      │
│                │  │  @Configuration │  │             │
│  - /signals    │  │                │  │  - findAll()│
│  - /location   │  │  - /ws         │  │  - save()   │
└───────┬────────┘  │  - /topic/*    │  └──────▲──────┘
        │           └────────────────┘         │
        │                                      │
┌───────▼──────────────────────────────────────┴──────┐
│                  Service Layer                      │
│                                                     │
│  processLocation(AmbulanceLocation):                │
│    1. Get all signals from DB                       │
│    2. Calculate distance (Haversine)                │
│    3. Find nearest signal                           │
│    4. If distance < 300m:                           │
│       - Set signal GREEN                            │
│       - Save to DB                                  │
│       - Broadcast via WebSocket                     │
│    5. Calculate time saved (AI)                     │
│    6. Return response                               │
└─────────────────────────────────────────────────────┘
```

---

## Database Schema

```
┌─────────────────────────────────────┐
│        TRAFFIC_SIGNAL               │
├─────────────────────────────────────┤
│  id (BIGINT) [PK, AUTO_INCREMENT]   │
│  latitude (DOUBLE)                  │
│  longitude (DOUBLE)                 │
│  status (VARCHAR) [RED/GREEN]       │
│  last_activated_time (TIMESTAMP)    │
└─────────────────────────────────────┘

Initial Data:
┌────┬──────────┬──────────┬────────┐
│ ID │ LATITUDE │ LONGITUDE│ STATUS │
├────┼──────────┼──────────┼────────┤
│ 1  │ 12.9716  │ 77.5946  │ RED    │
│ 2  │ 12.9750  │ 77.5980  │ RED    │
│ 3  │ 12.9800  │ 77.6000  │ RED    │
└────┴──────────┴──────────┴────────┘
```

---

## Algorithm Flow

### Haversine Distance Calculation
```
Input: lat1, lon1, lat2, lon2

Step 1: Convert to radians
  φ1 = lat1 × π/180
  φ2 = lat2 × π/180
  Δφ = (lat2-lat1) × π/180
  Δλ = (lon2-lon1) × π/180

Step 2: Calculate
  a = sin²(Δφ/2) + cos(φ1) × cos(φ2) × sin²(Δλ/2)
  c = 2 × atan2(√a, √(1-a))
  
Step 3: Distance
  distance = R × c  (R = 6,371,000 meters)

Output: distance in meters
```

### Signal Priority Logic
```
FOR each signal in database:
  distance = haversine(ambulance, signal)
  
  IF distance < 300:
    signal.status = "GREEN"
    signal.lastActivatedTime = now()
    save(signal)
    broadcast(signal)  // WebSocket
  ELSE:
    signal.status = "RED"
    save(signal)
    broadcast(signal)
    
  IF distance < minDistance:
    minDistance = distance
    nearestSignal = signal
```

### AI Time Estimation
```
currentTime = now()

IF currentTime between 17:00 and 21:00:
  trafficFactor = 2  // Peak hours
ELSE:
  trafficFactor = 1  // Off-peak

baseDelay = 2.0  // minutes

IF distance < 300:
  estimatedTimeSaved = baseDelay + trafficFactor
ELSE:
  estimatedTimeSaved = 0
```

---

## WebSocket Protocol

```
Client                          Server
  │                               │
  │──── CONNECT /ws ──────────────►│
  │                               │
  │◄─── CONNECTED ────────────────│
  │                               │
  │──── SUBSCRIBE ────────────────►│
  │     /topic/signals            │
  │                               │
  │◄─── SUBSCRIBED ───────────────│
  │                               │
  │                               │
  │◄─── MESSAGE ──────────────────│
  │     {                         │
  │       "id": 1,                │
  │       "status": "GREEN",      │
  │       "latitude": 12.9716,    │
  │       "longitude": 77.5946    │
  │     }                         │
  │                               │
```

---

## Request/Response Examples

### GET /api/signals
```json
Response: 200 OK
[
  {
    "id": 1,
    "latitude": 12.9716,
    "longitude": 77.5946,
    "status": "RED",
    "lastActivatedTime": null
  },
  {
    "id": 2,
    "latitude": 12.9750,
    "longitude": 77.5980,
    "status": "RED",
    "lastActivatedTime": null
  }
]
```

### POST /api/ambulance/location
```json
Request:
{
  "lat": 12.9716,
  "lng": 77.5946,
  "speed": 45
}

Response: 200 OK
{
  "nearestSignalId": 1,
  "signalStatus": "GREEN",
  "distance": 120,
  "estimatedTimeSaved": 4.0
}
```

---

## Technology Stack Details

```
┌─────────────────────────────────────────────────────────┐
│                    FRONTEND                             │
├─────────────────────────────────────────────────────────┤
│  React 19          - UI Framework                       │
│  TypeScript        - Type Safety                        │
│  Vite              - Build Tool                         │
│  Tailwind CSS      - Styling                            │
│  Leaflet           - Maps                               │
│  Axios             - HTTP Client                        │
│  SockJS            - WebSocket Polyfill                 │
│  STOMP             - WebSocket Protocol                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    BACKEND                              │
├─────────────────────────────────────────────────────────┤
│  Java 17           - Programming Language               │
│  Spring Boot 3.1.5 - Framework                          │
│  Spring Web        - REST API                           │
│  Spring Data JPA   - Database Access                    │
│  Spring WebSocket  - Real-time Communication            │
│  H2 Database       - In-Memory Database                 │
│  Lombok            - Boilerplate Reduction              │
│  Maven             - Build Tool                         │
└─────────────────────────────────────────────────────────┘
```

---

**System is production-ready and hackathon-optimized! 🚀**
